# Credenciales de prueba

Esta aplicación NO tiene autenticación en esta versión (§1, §16 del maestro):
un solo usuario (el director), sin cuentas. Los clientes son "espacios", no cuentas.

No hay credenciales. Se accede directamente a la aplicación.

- Frontend/backend (preview): https://recorridos.preview.emergentagent.com
- MongoDB local: mongodb://localhost:27017, base de datos `studio`.
